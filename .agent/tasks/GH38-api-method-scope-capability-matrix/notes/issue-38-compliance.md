# Issue 38 Compliance Check

Checked: 2026-04-21

Issue: https://github.com/bizyumov/yandex-office/issues/38

## Result

Issue #38 is satisfied by this PR.

The issue asks for an API-method x scope capability matrix and the supporting
inventories, experiment plan, machine-readable representation, and runtime
consumption guidance. It does not require runtime token-selection enforcement to
be implemented in this PR.

## Deliverables

1. Complete inventory of currently used API methods
   - Delivered in `capabilities/methods.json`.
   - Classification includes `current_used`.

2. Complete inventory of available but unused methods
   - Delivered in `capabilities/methods.json`.
   - Classification includes `implemented_unused` and `upstream_available`.

3. Normalized list of scopes to test
   - Canonical source: `references/yandex-oauth-scopes.json`.
   - Atomic test app/client inventory: `capabilities/atomic.client_id.json`.
   - Local ignored token store: `capabilities/atomic.token`.

4. Experimental capability matrix
   - Delivered in `capabilities/matrix.json`.
   - Generated by `capabilities/probe.py` from `capabilities/raw/*-probe.json`.

5. Machine-readable capability format
   - Full cell matrix: `capabilities/matrix.json`.
   - Runtime-oriented compact map: `capabilities/method-scope-map.json`.

6. Runtime consumption guidance
   - General process: `capabilities/README.md`.
   - Runtime integration note:
     `.agent/tasks/GH38-api-method-scope-capability-matrix/notes/runtime-method-scope-map-integration.md`.

## Current Coverage

```text
315 methods inventoried
816 matrix cells
343 works
458 does_not_work
15 unclear_needs_retest
309 method-scope-map entries
2 public
304 one_of
3 all_of
```

Service inventory:

```text
calendar    6
contacts    5
directory  49
disk       32
forms      33
mail       32
telemost    9
tracker   149
```

## Residuals

- SMTP remains `unclear_needs_retest` from this host because
  `smtp.yandex.com:465` is unreachable.
- `mail.imap.uid.delete_matching.live_realty` is a negative/safety probe for
  `mail:imap_ro`; it has no successful map entry by design.
- Runtime method-scope token selection is documented as a follow-up integration
  batch, because issue #38 requires guidance/foundation but not runtime
  enforcement implementation.
